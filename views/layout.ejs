<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MailMicro Admin Dashboard</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
    <div class="container">
      <a class="navbar-brand" href="/dashboard">MailMicro</a>
      <div>
        <a href="/smtp" class="nav-link d-inline">SMTP</a>
        <a href="/templates" class="nav-link d-inline">Templates</a>
        <a href="/campaigns" class="nav-link d-inline">Campaigns</a>
        <a href="/dashboard" class="nav-link d-inline">Dashboard</a>
        <a href="/logout" class="btn btn-sm btn-outline-light ml-3">Logout</a>
      </div>
    </div>
  </nav>
  <div class="container mt-4">
    <% if (success && success.length) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <%- body %>
  </div>
</body>
</html>